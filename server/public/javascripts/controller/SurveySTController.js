FeedbackSystem.SurveySTController = (function() {
var that = {},
init = function() {
  	console.log("SurveySTController init");

    //$(document).on("initSTControls", onInitControls);
    //$(document).on("getTripId", onGetTripId);
   
};

that.init = init;
return that;

}());